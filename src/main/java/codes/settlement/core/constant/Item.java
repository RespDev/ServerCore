package codes.settlement.core.constant;

import codes.settlement.core.util.ItemUtil;
import org.bukkit.Material;
import org.bukkit.inventory.ItemStack;

public class Item {

    /*
     * Constant items for the Core Plugin
     */
    public static ItemStack backButton = ItemUtil.create(Material.ARROW, "&cBack");
    public static ItemStack closeButton = ItemUtil.create(Material.BARRIER, "&cClose");
}
